import {Router} from 'express';import * as c from '../controllers/product.controller';import {authenticate} from '../middleware/auth';import {allowRoles} from '../middleware/roles';import {validate} from '../middleware/validator';import {productRules} from '../validations';const r=Router();r.use(authenticate);r.get('/',c.list);r.get('/:id',c.get);r.post('/',allowRoles('Admin','Warehouse'),productRules,validate,c.create);r.put('/:id',allowRoles('Admin','Warehouse'),productRules,validate,c.update);r.delete('/:id',allowRoles('Admin','Warehouse'),c.remove);export default r;
