import {Router} from 'express';import * as c from '../controllers/inventory.controller';import {authenticate} from '../middleware/auth';import {allowRoles} from '../middleware/roles';import {validate} from '../middleware/validator';import {inventoryRules} from '../validations';const r=Router();r.use(authenticate);r.get('/',c.list);r.post('/movements',allowRoles('Admin','Warehouse'),inventoryRules,validate,c.adjust);export default r;
