import {Router} from 'express';import * as c from '../controllers/challan.controller';import {authenticate} from '../middleware/auth';import {allowRoles} from '../middleware/roles';import {validate} from '../middleware/validator';import {challanRules} from '../validations';const r=Router();r.use(authenticate);r.get('/',c.list);r.get('/:id/pdf',c.pdf);r.get('/:id/invoice.pdf',c.pdf);r.get('/:id',c.get);r.post('/',allowRoles('Admin','Sales','Accounts'),challanRules,validate,c.create);r.patch('/:id/cancel',allowRoles('Admin','Sales','Accounts'),c.cancel);export default r;
